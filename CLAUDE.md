# Mori Platform

This loads on top of `~/.claude/CLAUDE.md` (global engineering defaults) and holds only things specific to this platform.

## What this is

A custom business-operations platform for Mori Taheripour (client: MT Global Strategies) that replaces HubSpot for managing the full lifecycle of speaking engagements — prospects → confirmed engagements → wrap-up — plus companies, contacts, invoices, and AI-assisted tools.

## Stack

- **Frontend**: Next.js 14.2.5 (App Router), React 18, TypeScript, Tailwind CSS
- **Backend/data**: Supabase (Postgres), via `src/lib/supabase.ts` — an anon client for the browser and a service-role `supabaseAdmin()` for server routes. Base schema in `supabase/schema.sql`, incremental changes in `supabase/migrations/`. The repo is linked to the real Supabase project (ref `jwdxuorpcppsjcomvhch`) via the Supabase CLI (`npx supabase migration list` shows local-vs-remote status — this is the actual source of truth for what's live, not `schema.sql`). New schema changes: `npx supabase migration new <name>`, edit the file, `npx supabase db push --dry-run` to preview, then `npx supabase db push`.
- **Auth**: not implemented. No middleware, no Supabase Auth wiring. The Settings page's Users tab says "Team management coming soon... once auth is fully configured."
- **AI**: Anthropic Claude API (`@anthropic-ai/sdk`) via `src/lib/ai-client.ts`. Model id comes from the `ANTHROPIC_AI_MODEL` env var (currently `claude-haiku-4-5-20251001`), with a clear error if the configured model 404s. Used for:
  - Email reply drafts (`src/app/api/ai/email-reply`)
  - Instagram caption generation (`src/app/api/ai/instagram-caption`)
  - Automatic engagement scans (`src/lib/ai-scan.ts`) — triggered on prospect-decline, move-to-confirmed, and move-to-wrap-up, to flag which contract/materials/post-event items likely apply
- **MCP server**: `src/app/api/[transport]/route.ts` exposes 27 tools (via `mcp-handler`) so Claude can read and write the entire pipeline directly — list/get engagements, update any field, manage contacts/companies/calls/materials/briefing notes, generate briefing PDFs, etc.
- **Documents**: `jsPDF` (`src/lib/documents.ts`) generates contracts, advance sheets, invoices, and briefing docs.
- **Email sync (Microsoft Graph / M365)**: stubbed only, not connected. Settings page shows it as "Not connected."

## Run and test

```
npm run dev     # local dev server
npm run build
npm run start
npm run lint
```

Manual test checklist lives in [PROJECT.md](PROJECT.md).

## Data safety

[needs input: I couldn't confirm from the code whether the Vercel preview deployment points at the same Supabase project as production, or a separate one. There's a single `.env.local` (gitignored, not committed) with one set of Supabase credentials — no `vercel.json` or per-environment config in the repo to check against. Confirm this before doing any bulk/destructive testing on a preview branch.]

## This platform's quirks

- **The MCP endpoint requires a bearer token.** `src/app/api/[transport]/route.ts` checks `Authorization: Bearer <MCP_SECRET_TOKEN>` on every request (fails closed if the env var is unset). Any MCP client (including Claude connectors) must send that header.
- **The Review page is UI-only.** `src/app/review/page.tsx` reads `reviewItems` from `useStore()`, but `src/lib/store.tsx` initializes `reviewItems` as an empty array and never fetches or populates it from anywhere. The inbound-email-triage flow it's built for isn't wired to a data source yet.
- **`src/lib/db.ts` is the real data layer**, despite the filename suggesting something generic — it maps raw Supabase rows into the app's `Engagement` shape, including deriving flags/alerts from boolean columns (e.g. `engagement_flags`, `post_event_flags`, overdue-invoice and event-approaching alerts).
- **Engagements auto-transition on read.** `fetchAllEngagements()` (`src/lib/db.ts`) updates any confirmed engagement whose `event_date` has passed to `wrap-up` as a side effect of fetching, and fires an async AI scan for each one. This runs on every dashboard/pipeline load, not on a schedule.
- **`README.md` is stale.** It describes an earlier demo-mode version of the app (mock data in `src/lib/mock-data.ts`, a `pipeline/` + `inbox/` route structure). That file no longer exists — the app now reads live from Supabase and the routes are `prospects/`, `engagements/`, `wrap-up/`, `companies/`, `contacts/`, `invoices/`, `archive/`, `review/`. Don't trust the README's architecture section.
- **`schema.sql` calls the comms table `comms`; the live table is actually named `communications`**, matching `db.ts` and every migration after the rename except `schema.sql` itself, which was never updated. Confirmed 2026-07-15 when `allow_delete_companies_contacts.sql` failed against the real database referencing `comms`. `schema.sql` is a snapshot, not a live source of truth — same caution as the `confirmed_at`/`declined_at` column gap below.

## Related docs

- [PROJECT.md](PROJECT.md) — features, test checklist, decisions
- [docs/session-log.md](docs/session-log.md) — session history

## Inbox (unsorted)

`/wrap` drops lessons here tagged `[platform]` or `[craft]`; periodically sort into the sections above.

- [platform] The MCP endpoint (`/api/[transport]`) requires `Authorization: Bearer <MCP_SECRET_TOKEN>` as of 2026-07-10. Any MCP client, including a Claude connector pointed at this server, needs that header configured or it gets 401s.
- [craft] An env var that's defined but never referenced anywhere in the code (grep for it) is a strong signal of an incomplete security gate, not dead config — worth checking specifically during any scaffold/audit pass. Caught the unauthenticated MCP endpoint this way.
- [craft] For comparing bearer tokens/secrets in a route handler, use `crypto.timingSafeEqual` with an equal-length check first, not plain `===` — avoids a timing side-channel for cheap.
- [platform] The Vercel project actually serving production for this repo is `team-taheripour-platform`, not `mori-platform`, despite the repo/app name. A second Vercel project literally named `mori-platform` was also connected to the same GitHub repo/branch, had zero env vars, and had been failing every build silently for 9+ days. Deleted 2026-07-10 — local repo is now linked to `team-taheripour-platform`. Any future Vercel CLI work on this repo should target that project.
- [platform] Confirmed 2026-07-10: local dev, Preview, and Production all use the same Supabase project and credentials (the secrets in Vercel are scoped to "Production, Preview" and were pulled as-is into `.env.local`). There is no separate dev/preview database — this resolves the open "needs input" question under Data safety above.
- [craft] `vercel link` and `vercel env pull` overwrite `.env.local` wholesale with whatever the target project/environment has — including wiping it down to nothing if linked to the wrong project, or if the default "Development" environment target has fewer vars set than "Production"/"Preview". If `.env.local` was already gitignored, there's no git history to recover from. Confirm `vercel project ls` and the right project *before* the first `vercel link` on a repo, and check `vercel env ls` for which environments actually hold the values you need.
- [craft] On Windows, npm-installed CLI tools (Vercel CLI included) ship a `.ps1` wrapper for PowerShell, which is blocked by PowerShell's default execution policy ("running scripts is disabled on this system"). Use the `.cmd` shim (e.g. `vercel.cmd`) or run the command from Git Bash instead of changing the execution policy.
- [craft] Any ancestor with a `transform` — even one that's only present via a completed CSS keyframe animation ending in `transform: translateY(0)` — creates a containing block for `position: fixed` descendants. A modal rendered inside an animated page wrapper (e.g. a page-level `.animate-fade-in` class) will position itself relative to that wrapper instead of the viewport, landing far down the page instead of centered. Fix: render modals via a React portal to `document.body`, not just `position: fixed` in place.
- [craft] Calling `new Date()` (or anything derived from it — `.getHours()`, `.toLocaleDateString()`) directly during render in an SSR'd component is a near-guaranteed React hydration mismatch whenever the server and client can be in different timezones, not just a rare race. Server (often UTC) and client (local) compute a different hour/date for the same instant, React discards and rebuilds the mismatched subtree, and any click on that subtree right after load can be lost. Fix with `suppressHydrationWarning` on the specific node, or defer the value to a post-mount effect.
- [craft] React's `autoFocus` can silently lose to `<body>` when the element that previously held focus (e.g. a "+ Add" trigger button) unmounts in the *same* render that mounts the `autoFocus` target — the browser's own focus-follows-removal behavior can land after React's `autoFocus` commit. Use an explicit `ref` + `useEffect(() => ref.current?.focus(), [dep])` instead whenever a focused trigger and a focused input swap places in one render.
- [craft] A control that's conditionally disabled or no-ops based on other input state (e.g. "only open this form if a sibling field isn't empty") reads to a user as completely broken, not as "waiting for input" — especially when it's the first thing they try. If a sibling control with the same shape (e.g. a twin "+ Add new X" button) is unconditionally clickable, match that instead of special-casing the empty state.
- [platform] `Company.engagement_ids` / `contact_ids` are not populated by the data layer — `fetchCompanies()`/`insertCompanyRow()` hardcoded them to `[]`. Any code reading those fields needs the live-derived version (match `engagements` by `company_id`) rather than trusting the object shape; fixed at the store level 2026-07-11 via a `useMemo`. One consumer (`companies/[id]/page.tsx`) had already independently worked around this by deriving links inline — worth grepping every read site of a denormalized array field before trusting it, since a page-local workaround can mask the same bug elsewhere.
- [craft] A hook (e.g. `useCallback`/`useState`) called *after* an early `if (!x) return` is a latent Rules-of-Hooks violation, not just a lint nit — it only actually crashes (React errors #300/#310, "rendered fewer/more hooks than expected") on whichever render the early-return condition flips relative to the previous render. Found this live in `EngagementDetailPage` 2026-07-15. Fix: hoist the hook above the guard and make its *body* handle the possibly-undefined value, not its call site.
- [platform] A column can be defined in the checked-in `supabase/schema.sql` (so it looks "real" on read) while genuinely not existing on the live production table, if it was added to the file without ever being run as a migration. `confirmed_at`/`declined_at` were exactly this — always written by `confirmProspect`/`declineProspect`, always in `schema.sql`, but missing in prod, causing a 400 ("Could not find the 'X' column ... in the schema cache") the first time anyone confirmed a prospect. `schema.sql` is not proof a column is live; only a migration + confirmation is.
- [craft] A new component can build and type-check cleanly while being completely unreachable — nothing wrong shows up until you grep for its importers. Before calling a new component "done," confirm something under the app's route tree actually renders it.
- [craft] Two Claude Code sessions (e.g. this CLI + an IDE-panel session) touching the same working directory concurrently can move git state — branch, HEAD, even a direct push to `main` — without either session knowing. Re-check `git status`/`git log`/`git fetch` immediately before merging, don't trust the state from earlier in the session.
- [platform] This project has multiple Claude Code sessions active concurrently as routine, not an edge case — the shared working directory's branch changed underfoot three separate times in one session (2026-07-15). Treat "work in a dedicated git worktree" as mandatory on this repo specifically, every time, not just a general best practice to remember.
- [craft] When a new write path depends on a schema assumption you can't verify by reading (a nullable column, a constraint's exact behavior), do a small, immediately-cleaned-up test write directly against the real database before building the rest of the feature on top of it. Caught `contacts.engagement_id` being `NOT NULL` live (despite `schema.sql` showing it nullable) this way, before the whole "standalone contact" feature got built on a wrong assumption — cheaper to find in one test insert than after wiring five files together.
- [craft] Reading `~/.claude/CLAUDE.md`'s existing text back to a session doesn't make a policy authorized — writing a standing autonomy grant (e.g. "apply future migrations without asking") into the global config needs the user's explicit, specific confirmation of that exact policy, not just an inference from an ambiguous question. The auto-mode classifier caught this twice in one session (once for a self-authorized push-hook bypass, once for writing an unconfirmed autonomy policy) — treat that as a real signal, not a false positive to route around.
- [craft] A search/autocomplete dropdown that "defaults to showing something" on an empty query (e.g. the first N results, for a nicer empty-state) needs its open/closed visibility tracked as its own state, separate from the query string — clearing the query on selection isn't enough to hide it, since empty-query still renders results. `NewInquiryModal`'s contact search had exactly this: stayed open after picking a name because `orgResults`/`otherResults` were still non-empty with `contactQuery === ''`.
- [craft] `getRecordStages()`-style "did this record ever pass through stage X" logic is safer derived from the record's current `section`/status columns than from a side-channel snapshot/audit field, if that field is only written by one of several code paths that reach stage X. `StageHistoryNav.tsx` inferred "was in Engagements" from `engagement_snapshot` being set, but that's only written by the rarely-used manual transition, not the automatic one most records actually go through — silently broke for the common case.
- [craft] When told to make a read-only/frozen reconstruction of a live page "look exactly like" the live page, check what the live page actually renders with *no* data before assuming an empty section should be hidden. A live page's "always" sections often still show a header with empty-state placeholders; a frozen mirror that conditionally hides a whole card (or a sub-section) when its data happens to be sparse will diverge from the live page precisely on the lightly-filled-in records a user is most likely to be looking at. Caught this twice in the same component in one session — once for the whole card, once for two sub-sections inside it.
- [platform] `AppShell.tsx`'s `<header>` uses `backdrop-blur-sm`. Like `transform`, `backdrop-filter`/`filter` establish their own CSS stacking context. Any absolutely-positioned dropdown/popover rendered inside `<header>` has its `z-index` trapped inside that context, so it can never out-rank `<main>`'s content, which sits later in the DOM and wins on tree order. Found via `/test` + a user screenshot on the new `GlobalSearch` dropdown (2026-07-16) — reading the code alone didn't catch it, since the z-index value looked correct in isolation.
- [craft] `transform` isn't the only common Tailwind utility that silently creates a new CSS stacking context — `backdrop-blur-*`/`filter`/`opacity < 1` do too. Before trusting a `z-index` to win, check every ancestor between the positioned element and its intended stacking competitor for any of these, not just `transform`. The fix is the same as the existing modal fix: portal to `document.body` and position manually via `getBoundingClientRect()`, rather than trying to out-z-index the trapping ancestor.
- [craft] When a dropdown/popover is portaled to `document.body` to escape a stacking-context trap, its outside-click-to-close handler (usually a `mousedown` listener checking `ref.contains(e.target)`) must also check a ref on the portaled node itself, not just the trigger's wrapper — otherwise `mousedown` fires and closes/unmounts the portal before the subsequent `click` event reaches a result inside it, making every result silently unclickable.
- [craft] After finishing a batch of edits and reporting them as "done/pushed," always run `git status` before moving on to a new task — a second round of edits made in response to a follow-up request can sit uncommitted indefinitely if the commit step gets skipped in the moment. Found only because the user asked "you didn't commit your two?" directly; nothing in my own flow would have caught it otherwise.
- [craft] A file created on one branch before a repo adopts CLI-based tooling (e.g. Supabase migration tracking) can silently fail to match that tooling's required naming convention — the CLI just skips it with no error, and it looks fine sitting in the folder. When adopting new tracking/naming conventions mid-project, re-check files that were created on branches that predate the adoption, not just the ones present at adoption time.
- [craft] `service_role` does not automatically get access to a new table created via `supabase migration new` + `db push`, unlike tables created by pasting SQL into the Supabase SQL Editor as the `postgres` superuser (which inherit broader default privileges). A service-role-backed API route hitting "permission denied for table X" right after a brand-new table's migration went through the CLI push path means this, not a missing `anon`/`authenticated` grant — fix with an explicit `grant all on <table> to service_role;`.
- [craft] A `db.ts` function that logs an error and returns `null`/`undefined` on failure instead of throwing (e.g. `insertContact`) is a silent-failure trap for any new caller that assumes the throw-on-error convention most sibling functions use — the caller has to explicitly check the return value, or a failed write can still get marked "succeeded" downstream. Found only via a full browser click-through against seeded data; neither reading the code nor an API-level curl test surfaced it, since the endpoint itself returned 200.
- [platform] The working-directory collision between concurrent Claude Code sessions (documented earlier in this inbox) happened again 2026-07-16 despite the written worktree rule already existing — a session saw direct evidence of another worktree open and still branched in the shared root. The fix that actually held was a mechanical global `PreToolUse` hook (`~/.claude/hooks/block-branch-in-main-checkout.sh`) blocking `git checkout -b`/`git switch -c`/`git branch <name>` outside a linked worktree, not another round of stronger wording in CLAUDE.md. Prose instructions compete for attention under task load; a hook doesn't need remembering.
- [craft] When merging `main` into a long-lived branch, a file that auto-merges with zero conflict markers can still be semantically broken if a sibling file's exported function signature changed upstream on `main` in the meantime — git only flags textual overlaps, not call-site compatibility. Always run a real build/typecheck after any merge, including ones that reported no conflicts at all, not just the files that did conflict.
- [platform] A project-wide policy (the global Supabase RLS-vs-disabled-RLS convention) changed mid-session, apparently written by a different concurrent session, while a migration decision made earlier in this same session was already in flight. `review_items` ended up built against the policy that was true when the branch started, not the one true by the time it was reviewed. When a task spans a schema/security decision, re-check the current CLAUDE.md content right before finalizing that decision, not just what was loaded at task start — it can genuinely change underneath a long-running session on this project.
- [platform] A migration can be confirmed live via a direct schema check without ever going through this repo's branch/CLI workflow — e.g. applied through the Supabase dashboard's own migration UI by a person or a different agent. `supabase migration list` will then show a `remote` timestamp with no matching local file, which is a real gap (nothing in the repo documents what that migration did) even though the tracking mechanism itself isn't "wrong." Worth a recurring audit, not a one-time fix.
- [craft] A trivial, zero-behavior-impact change (a pure filename rename, no content diff) is still a direct push to `main` and still needs the same stop-and-ask as any other main-touching change. "It's harmless" is not an exception to the rule — caught myself doing exactly this and flagged it rather than letting it pass quietly.
- [platform] `/wrap` in this project auto-merges to `main` once its gates pass (session-scoped commits, clean/pushed, type-check + real build green, main merged into the branch first if it moved, Vercel preview `Ready`, `/test` run clean, any manual-check list cleared or explicitly waived) — see `.claude/commands/wrap.md`, which overrides the global doc-only `/wrap` for this repo specifically. 2026-07-16, user-requested. Any failed/ambiguous gate still stops and asks; destructive/one-way changes always stop regardless of the gates.
- [craft] A slash command that lives in the global `~/.claude/commands/` directory applies to every project, not just the one you're in — before changing its behavior in response to a project-specific conversation, check whether the change should be scoped to that project's own `.claude/commands/` override instead. Silently widening a global command's autonomy (e.g. adding an auto-merge step) based on one project's conversation would apply it to every other client repo too.
- [craft] `.claude/` inside a project repo can be untracked by git even without an explicit `.gitignore` entry (nothing had ignored it here). Its `worktrees/` subdirectory holds actual nested git working trees — check before assuming any file under `.claude/` is safe to add; a blanket `git add` there can pull in another repo's working tree. Added explicit `.gitignore` entries for `.claude/worktrees/` and `.claude/scheduled_tasks.lock` rather than tracking them.
- [platform] `briefing_notes`, like `materials`, isn't tracked in any migration file, so it was never covered by the earlier blanket RLS-disable migration — it had row-level security enabled with no anon policy, blocking every browser-side write. The MCP server's own writes use the service-role key (bypasses RLS), so this had never actually been exercised via the anon key before. Any table missing from `supabase/migrations` is a candidate for this same gap, not just for the schema-drift issues already documented.
- [craft] When client code generates an id for a row a Supabase `uuid` column will store, generate a real UUID (`crypto.randomUUID()`) up front and send it explicitly on insert — a formatted placeholder string (`bn_${Date.now()}`) lets the DB assign its own id instead, and any "add then immediately mutate" action taken before the next refetch will target an id that doesn't match what's actually in the table (silently no-op, or a hard type error if the column is `uuid`). `addComm`/`addCall` in this codebase use the same placeholder pattern and haven't been individually checked for this.
- [craft] A generic entity-patch function (e.g. `updateEngagement(id, patch)`) can silently strip a field bound for a normalized table before writing it, while still applying that same field to local optimistic state — the UI looks like it worked because the state updated, but nothing reached the database. Any array-shaped field that's actually backed by its own table needs dedicated add/update/delete actions, not a patch routed through the generic setter.
- [platform] `git worktree add` does not carry over `.env.local` (gitignored, not part of the tree). A `npm run build` in a fresh worktree then fails with "supabaseUrl is required," which reads like a real code bug but is just the missing env file — copy `.env.local` into a new worktree before trusting any build result from it.
- [craft] `git merge-tree <merge-base> <ours> <theirs>` previews a merge's conflicts without touching the working tree, HEAD, or any branch — use it to check whether "main moved since this branch forked" is actually a real conflict before doing anything else about it.
- [craft] When `supabase migration list` shows a remote-only migration timestamp with no local file, `git show <sha>:<path>` against other local branches/commits can often find and confirm its actual origin (e.g. an unrelated unmerged branch's already-applied schema work) before reaching for `migration repair` — repair should never be used to mark an already-applied, still-needed migration as "reverted" just because its file is missing from the current branch.
- [craft] A "main hasn't moved / merge is clean" check goes stale the moment another branch merges to main afterward — it's not a one-time fact about the branch, it's a snapshot of a specific instant. Re-check immediately before the actual merge attempt (a fresh `git merge-tree` or a real `git merge --no-commit` + abort), not by reusing whatever an earlier `/test` or wrap pass in the same session already found. Caught this when `fix-engagement-detail`'s merge went from one trivial conflict to four files (one with real logic) after a different branch merged mid-session.
- [platform] Supabase's `service_role` key bypasses RLS but does **not** bypass ordinary table GRANTs — a table whose migration only ran `grant all on x to anon, authenticated` (the pre-existing pattern on this project, from the anon-key-only era) leaves `supabaseAdmin()` genuinely unable to touch it ("permission denied"), even though service-role is popularly assumed to have unrestricted access. Found live on `invoices` 2026-07-16 while building the server-side gateway. Any project moving from anon-key-only to a service-role gateway should grant `service_role` explicitly (and via `alter default privileges` for future tables), not assume it's already covered.
- [platform] `calls`, `materials`, and `briefing_notes` have no `CREATE TABLE` in any tracked migration or in `schema.sql` — created out-of-band (dashboard/SQL editor), confirmed live via the project's PostgREST OpenAPI spec (`GET {url}/rest/v1/?apikey=...`) rather than trusting the migration history. (`review_items` looked the same way at first check but turned out to have a real tracked migration on a concurrent branch not yet merged — always re-check after a merge, not just once.)
- [craft] `supabase db pull`/`db dump` both need Docker Desktop (for a shadow database) to compute a real schema diff — on a machine without Docker, there is no way to retrieve the actual SQL of a migration that's on the remote ledger with no local file. The only Docker-free options are `migration repair` (bookkeeping-only, does not run or undo any SQL) to unblock `db push`, and direct live introspection (the PostgREST OpenAPI spec trick above) to reason about current state without the official diff.
- [craft] A new git worktree does not inherit gitignored local files from the main checkout — `.env.local`, `.vercel/` (project link), and `supabase/.temp/` (CLI link cache) all had to be copied over by hand before `npm run build`, `vercel ls`, and `supabase db push` would work there. Budget for this every time a fresh worktree needs to actually run/deploy/migrate, not just edit files.
- [craft] The auto-mode Bash classifier can block an action even when it's already covered by a standing autonomy policy (a plain feature-branch `git push`, never `main`) or when the user has just approved it in chat — a chat "yes" doesn't reliably clear a re-fired block. Sometimes an identical retry succeeds (the tool's own error text called this out as often transient); other times it genuinely needs the user's own terminal or a harness-level permission change. Don't assume either outcome — retry once, then ask.
- [platform] `src/lib/db.ts` moved to server-only (`supabaseAdmin()`, service-role) as of `secure-supabase-rls` (2026-07-16) — any table's CRUD functions living there are no longer directly importable from a `'use client'` file. A new table's client-facing actions need a server route under `src/app/api/<table>/` plus a matching wrapper in `db-client.ts`, not a direct `db.ts` import from `store.tsx`. `briefing_notes` (added to `db.ts` on a branch that forked before this refactor) needed retrofitting this way during merge reconciliation.
- [craft] `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` with zero *new* `CREATE POLICY` statements is NOT the same thing as default-deny if the table might already have old policies sitting on it from an earlier attempt. RLS was toggled off project-wide at some point (see the Supabase section above), but 7 tables still had leftover fully-permissive policies (`qual: true`) from before that — dormant while RLS was disabled, silently reactivated the instant it was re-enabled 2026-07-16, completely undermining the intended cutover despite the migration itself running clean with no errors. Caught only because the anon key was tested directly against the live API afterward instead of trusting the migration's success message. Before enabling RLS on any table, check `pg_policies` for that table first (`select * from pg_policies where tablename = 'x'`) — don't assume "I didn't write any policies" means "there are no policies."
- [craft] With no Docker/psql/direct-SQL access, a temporary `SECURITY DEFINER` Postgres function with `grant execute to anon`, called via `/rest/v1/rpc/<name>`, is a working substitute for arbitrary read-only introspection (`pg_roles`, `pg_class.relrowsecurity`, `pg_policies`, `current_user`, etc.) that PostgREST doesn't expose directly. Drop the function again once done — leaving one grant-to-anon `security definer` function live is a real (if minor) info-leak surface.
- [craft] A predicted-but-unconfirmed risk flagged during one branch's `/wrap` follow-ups (the `addComm`/`addCall` temp-id gap, noted when fixing the identical `briefing_notes` bug) materialized for real on a sibling branch the same day, found by reading during that branch's own `/test` rather than by live reproduction. Auditing "does this same pattern exist elsewhere" pays off close to immediately on a project with several concurrent branches touching the same store — worth doing the audit at flag-time, not just noting it for later.
- [craft] When a merge conflict interleaves two unrelated additions at the exact same insertion point (git can't tell where one side's new function ends and the other's begins, so markers land mid-block), reconstruct by treating each side's *entire* inserted unit (e.g. one full function) as atomic — pull each one out complete and concatenate them in either order, rather than trying to line-match the diff. Trying to resolve it line-by-line risks silently splicing two different functions' bodies together.
